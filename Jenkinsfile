pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo 'Jenkins is connected to GitHub!'
                sh 'docker --version'
            }
        }
    }
}
